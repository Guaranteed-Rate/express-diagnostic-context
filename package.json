{
  "name": "express-diagnostic-context",
  "version": "0.0.1",
  "description": "Middleware that provides access to diagnostic info such as RequestId for use in loggers and passing to remote calls",
  "main": "edc.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "continuation-local-storage": "~3.1.4",
    "cls-middleware": "~1.1.0"
  }
}
